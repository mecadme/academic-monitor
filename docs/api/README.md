# API

La API REST queda versionada desde el inicio bajo `/api/v1`.

Endpoint disponible en el bootstrap:

```http
GET /api/v1/health
```

OpenAPI/Swagger queda incluido en el backend para documentar endpoints conforme se agreguen nuevos modulos.
